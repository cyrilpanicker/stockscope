{
  "name": "stockscope",
  "version": "0.0.1",
  "author": "cyrilpanicker@gmail.com",
  "main": "build/server/main.js",
  "scripts": {
	  "remove:build:server":"rimraf ./build/server",
	  "compile:server":"tsc ./source/server/* --outDir ./build/server",
	  "build:server":"npm run remove:build:server && npm run compile:server",
	  "postinstall": "npm run build:server",
	  "start":"node ./build/server/main.js"
  },
  "devDependencies": {
    "concurrently": "^2.1.0",
    "mkdirp": "^0.5.1",
    "ncp": "^2.0.0",
    "rimraf": "^2.5.2",
    "typescript": "^1.8.10",
    "typings": "^1.3.0"
  }
}
